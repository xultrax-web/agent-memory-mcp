# Normalize line endings: text files are stored and checked out as LF
# regardless of each contributor's core.autocrlf setting. Prevents CRLF from
# Windows checkouts leaking into commits (which fails `prettier --check`).
* text=auto eol=lf
